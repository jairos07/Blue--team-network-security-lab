# Wazuh indexer credentials
# Copy this file to .env and set real values before running docker-compose
# The .env file is ignored by git — never commit it

INDEXER_USERNAME=admin
INDEXER_PASSWORD=your_password_here
